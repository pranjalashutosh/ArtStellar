# Server Configuration
NODE_ENV=development
PORT=5000
APP_URL=http://localhost:5000

# Database
# PostgreSQL connection string (required for production)
# Example: postgresql://user:password@host:5432/database
DATABASE_URL=

# Session
# Generate a strong random secret (minimum 32 characters)
# You can use: openssl rand -base64 32
SESSION_SECRET=your-super-secret-random-string-min-32-chars-change-this

# Stripe (Payment Processing)
# Get these from: https://dashboard.stripe.com/apikeys
# Use test keys (sk_test_...) for development
# Use live keys (sk_live_...) for production
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe Webhook Secret
# Get this after creating webhook endpoint
# Development: Use Stripe CLI (stripe listen)
# Production: Get from Stripe Dashboard webhook settings
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Shipping Configuration
# Flat rate shipping cost in cents (default: 1500 = .00)
SHIPPING_FLAT_RATE_CENTS=1500

# Free shipping threshold in cents (default: 15000 = .00)
# Set to 0 to disable free shipping
FREE_SHIPPING_THRESHOLD_CENTS=15000

# Email Configuration (Optional - for Task 5.3)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your_sendgrid_api_key
# FROM_EMAIL=noreply@yourdomain.com
# FROM_NAME=Prana Art Gallery

# Admin User (for initial setup)
# ADMIN_EMAIL=admin@yourdomain.com
# ADMIN_PASSWORD=your-secure-admin-password

# File Storage (Optional - for production with S3/R2)
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_BUCKET_NAME=
# S3_REGION=us-east-1
# S3_ENDPOINT= # For Cloudflare R2 or compatible services

# Logging (Optional)
# LOG_LEVEL=info
